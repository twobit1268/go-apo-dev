server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback: any unmatched path serves index.html so client-side
    # routing (e.g. /parts/123) works on a hard refresh.
    location / {
        try_files $uri /index.html;
    }
}
